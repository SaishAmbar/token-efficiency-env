"""Phase 7 training notebooks + their helper modules.

This package only exists so ``train_grpo.ipynb`` can do
``from notebooks.eval_baseline_vs_trained import evaluate, ...`` without
having to mess with ``sys.path`` from inside a notebook cell. There is no
runtime code here.
"""
